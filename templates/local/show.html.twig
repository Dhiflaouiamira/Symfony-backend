{% extends 'base.html.twig' %}

{% block title %}Local{% endblock %}

{% block body %}
    <h1>Local</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ local.id }}</td>
            </tr>
            <tr>
                <th>Maitrage</th>
                <td>{{ local.Maitrage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_local_index') }}">back to list</a>

    <a href="{{ path('app_local_edit', {'id': local.id}) }}">edit</a>

    {{ include('local/_delete_form.html.twig') }}
{% endblock %}
